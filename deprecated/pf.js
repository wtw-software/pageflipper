(function( $ ) {

   var pf;

   pf = (function( ) {
      var _settings = { //defaults
      
      };
      return {
         init: function( settings ) {
         },
         destroy: function() {
         }
      }
   })( );

})( jQuery );

 